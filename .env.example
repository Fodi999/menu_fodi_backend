# Backend Configuration

# Server
PORT=8080

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-secret-key-change-this-in-production

# Database - Neon PostgreSQL (Pooled connection - рекомендуется)
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require"

# Для операций требующих прямого подключения (миграции, бекапы)
DATABASE_URL_UNPOOLED="postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require"

# CORS Origins (разрешённые домены для API)
ALLOWED_ORIGINS="http://localhost:3000,https://your-app.vercel.app"
